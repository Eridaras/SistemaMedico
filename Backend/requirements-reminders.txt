# Requirements for Email and WhatsApp Reminders

# Twilio SDK for WhatsApp messaging
twilio==9.0.0

# All other requirements are already in base requirements.txt:
# - Flask (for web service)
# - psycopg2-binary (for PostgreSQL)
# - python-dotenv (for .env configuration)
# - smtplib is part of Python standard library
